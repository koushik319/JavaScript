// Temoprial Dead Zone 
console.log(a);


// Temporal Dead ZOne - The time between the hositing of let variable and getting it initialized 

// The phase from hoisting till it gets initialization is known as Temporal Dead Zone.

let a=10;

var b=10;
console.log(b);